import styled from "styled-components";

export const CardJogador = styled.div`
  background: rgba(0, 0, 0, 0.2);
  border-radius: 10px;
  padding: 0.5rem 0;
  vertical-align: middle;
  box-shadow: rgba(255, 255, 255, 0.01) 1.95px 1.95px 2.6px;

  img {
    border-radius: 50%;
  }
  .valor{
    font-size: .9em;
  }
`;
